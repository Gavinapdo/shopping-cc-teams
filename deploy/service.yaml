# 商品管理系统后端 Service 配置
apiVersion: v1
kind: Service
metadata:
  # 服务名称
  name: shopping-backend-svc
spec:
  # 服务类型
  type: ClusterIP
  # 选择器，匹配 Deployment 中的 Pod 标签
  selector:
    app: shopping-backend
  ports:
    - protocol: TCP
      # 服务端口
      port: 8080
      # 目标容器端口
      targetPort: 8080
